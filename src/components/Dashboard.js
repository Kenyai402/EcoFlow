import React from 'react';
import './Dashboard.scss';

const Dashboard = () => {
    return (
        <div className="dashboard">
            <h2>Dashboard</h2>
            <p>View real-time statistics and waste collection insights.</p>
            {/* Future dashboard elements go here */}
        </div>
    );
};

export default Dashboard;
