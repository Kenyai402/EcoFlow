import React from 'react';
import './Header.scss'; // Create a CSS file for Header styles

const Header = () => {
    return (
        <header className="header">
            <h1>Welcome to EcoFlow</h1>
        </header>
    );
};

export default Header;
